一、改变this指向函数
	改变函数this指向三个方法
		bind call apply

		三个方法的相同点：
			目标函数被调用时，改变this的指向为指定的值
			三个方法都是函数的方法，挂载在Function.prototype上


		不同点：
			目标函数调用call和apply后，会直接被执行
			目标函数调用bind后，不会立即执行，而是返回一个新的函数，
			调用新函数才会执行目标函数